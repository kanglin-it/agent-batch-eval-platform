# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Data / ORM
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0        # PostgreSQL driver (login DB + case-data DB)

# Auth: verify Django password hashes + issue our own JWT
passlib[bcrypt,argon2]==1.7.4
pyjwt==2.10.1

# HTTP client for the Zhiexa Agent integration (SSE streaming)
httpx==0.28.1

# AES decrypt for SaaS OSS encrypted review files (.txt ciphertext)
pycryptodome==3.21.0

# Excel export (评测结果 下载)
openpyxl==3.1.5

# Validation (schemas). Config is read from config.ini via stdlib configparser.
pydantic==2.10.4
python-multipart==0.0.20
